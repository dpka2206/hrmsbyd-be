# Database (PostgreSQL is default; set USE_SQLITE=1 for local dev without Postgres)
# DB_NAME=hrms
# DB_USER=postgres
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=5432

# Optional: use SQLite for local development
# USE_SQLITE=1

# Optional: timezone (default UTC)
# TIME_ZONE=UTC
